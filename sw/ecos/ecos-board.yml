schema: 1
id: mpc-soc
name: MPC SoC
aliases:
  - mpc
  - mpc-soc
category: mpc-soc
arch: riscv

files:
  makefile: Makefile
  board_header: board.h
  linker_script: sections.lds
  startup: start.S
  build_conf: build_conf.mk

kconfig:
  board: board.kconfig
  drivers: driver.kconfig

paths:
  loader: loader
  templates: templates
